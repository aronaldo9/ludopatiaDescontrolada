{% extends 'base.html.twig' %}

{% block title %}Apuesta{% endblock %}

{% block body %}

    <h1>Apuesta</h1>

    <table class="table">
        <tbody>
            <tr>
                <td>{{ apuestum.ticket.number }}</td>
                <td>{{ apuestum.sorteo.nombre }}</td>
                <td>{{ apuestum.sorteo.getPrecioPapeleta() }}</td>
            </tr>
           
        </tbody>
    </table>
 {% if not apuestum.usuario %}
    <a href="{{ path('app_apuesta_comprar', {'id': apuestum.id, 'precio': apuestum.sorteo.getPrecioPapeleta()}) }}">comprar</a>

  <a href="{{ path('app_main') }}">home</a>
  {% endif %}
{% endblock %}
